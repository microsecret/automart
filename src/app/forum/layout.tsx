import "./forum-page.css"

/**
 * Раскладка раздела форума.
 *
 * Нужна только затем, чтобы подключить стили раздела: они вынесены из
 * общего файла, который грузится на каждой странице сайта. Замер
 * покрытия главной показал, что из двухсот килобайт globals.css
 * применялось сорок один, и стили форума были частью неиспользуемого.
 */
export default function ForumLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
