-- Город посещения.
--
-- Владелец спрашивает, из каких городов заходят. По хешу адреса этого не
-- узнать — хеш необратим, и это правильно. Но город, выбранный человеком
-- на карте заправок или в фильтре каталога, честнее любой геобазы: он
-- говорит, где человек ищет машину, а не откуда подключился.
--
-- Поле необязательное: у прежних событий его нет и взяться неоткуда.
ALTER TABLE "VisitEvent" ADD COLUMN "city" TEXT;

CREATE INDEX "VisitEvent_city_createdAt_idx" ON "VisitEvent"("city", "createdAt");
