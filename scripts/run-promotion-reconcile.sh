#!/usr/bin/env bash
set -euo pipefail

# Сверка оплат продвижения с кассой.
#
# Кто и что активировать, решает приложение: сверяются только заказы со
# статусом «ждёт оплаты», сумма платежа проверяется до копейки, а
# активация идёт тем же кодом, что и по уведомлению кассы. Здесь только
# регулярный вызов.
cd "$(dirname "$0")/.."
set -a
# Продакшн-.env живёт на сервере и не коммитится.
source ./.env
set +a

node scripts/reconcile-promotions.mjs
